# Godot cache and generated files
.godot/                            # Godot 4.x internal cache (should not be versioned)
.import/                           # Imported asset metadata
.cache/                            # Old Godot cache dir (still sometimes used)
*.import                           # Extra safety for stray import files
*.uid                              # Godot unique resource IDs

# Godot config files you may want to ignore
export.cfg
export_presets.cfg
godot.env
.editor_settings-*.json           # Local editor settings (machine-specific)

# OS-specific files
.DS_Store
Thumbs.db
desktop.ini

# User-specific temp/log/backup files
*.log
*.tmp
*.temp
*.bak

# IDE/project folders (optional: remove if you want to include settings)
.vscode/
.idea/
